# Setup Instructions

## Client

1. Navigate to the `client` folder
2. Execute `npm i`
3. Execute `npm run build` to generate the `/dist` folder

## Server

1. Navigate to the root folder
2. Execute `npm i`
3. Execute `nodemon server.ts` to start the back-end server
